# 多模态传感器数据处理复刻学习路线

## 1. 这份路线要解决什么问题

目标不是“把学长的两个脚本点一下运行”，而是做到四层复刻：

1. **结果复刻**：能从 120 个原始 CSV 得到同样的处理后 CSV、波形图和对齐数据。
2. **方法复刻**：能解释每一个参数、公式和算法为什么存在。
3. **验证复刻**：能用频谱、波形、统计指标证明处理确实有效，而不是只看“程序没报错”。
4. **独立改进**：换一批数据、改一个采样率或条件后，能够自己调整参数、发现问题并评价改进是否有效。

建议把“学会”定义为：**不看原脚本，也能独立写出单文件处理流程；能解释每一步的输入、输出、目的和风险；能用定量指标验收；能把流程迁移到新数据。**

---

## 2. 先认识当前项目

### 2.1 仓库中已有内容

| 内容 | 数量/规格 | 作用 |
|---|---:|---|
| 原始 CSV | 120 个 | 20 次重复 × 6 个条件：900、1120、1340、1560、1780、2000 |
| 每个 CSV | 10000 行 | 按脚本设定的 2000 Hz 采样率，对应 5 s |
| 原始列 | `T`, `AI2_filter1` | `T` 实际为 0~9999 的采样点编号，不是秒；第二列是待处理电信号 |
| `Batch_Process_Piezo.m` | 1 个 | 去直流、50 Hz 陷波、20 Hz 低通、Hilbert 包络、滑动 RMS、批量保存 |
| `data1.m` | 1 个 | 用“首次边缘触发”找动作起点，截取并对齐 1 s 数据 |
| 学位论文 PDF | 74 页 | 理解压电纤维原理、制备、应用场景和论文结论 |

本次快速审计发现：原始数据的主频均为 **50 Hz**，45~55 Hz 频带约占原始信号频谱能量的 91%~92%。因此，当前流程中的 50 Hz 陷波不是装饰步骤，而是从强工频干扰中恢复低频动作信号的关键步骤。

### 2.2 目前必须补齐的信息

项目里没有数据字典，以下内容不能靠猜：

- 文件名中的 900、1120、1340、1560、1780、2000 分别代表什么，单位是什么；
- 1~20 是重复试验编号、受试者编号，还是其他含义；
- `AI2_filter1` 的传感器来源、单位、量程和采集卡型号；
- 文件夹名虽为 `FBG+PIO`，但当前 CSV 只有一个信号通道；FBG 数据在哪里、如何同步；
- 每次动作的真实开始时刻、动作类别和实验控制条件；
- 采集时是否已经做过模拟或数字滤波，`filter1` 的具体含义是什么。

这些信息应向学长确认后写入 `README.md` 或 `data_dictionary.md`。在此之前，六个数字只能称为“条件标签”，不能写成速度、角度、载荷或频率。

### 2.3 论文与当前 CSV 的关系边界

论文提供的是传感器物理背景和应用证据，不是当前两段 MATLAB 代码的逐行来源：

- 论文呼吸实验使用 SS-C202 采集电路、SerialPlot，采样率为 1300 Hz；
- 论文水下实验是 1~20 MHz 量级的超声测试，并用示波器和 FFT 分析；
- 当前 CSV 脚本固定采用 2000 Hz，并保留 20 Hz 以下的低频成分，脚本注释说明其面向手势数据。

因此，应当借论文理解“压电纤维为何能把机械形变转换为电信号”，但不能把论文的采样参数直接套用到当前数据。

---

## 3. 软件与环境

### 3.1 必需软件

1. **MATLAB**：建议 R2022b 或更新版本。
2. **Signal Processing Toolbox**：脚本使用了 `iirnotch`、`butter`、`filtfilt` 和 `hilbert`。
3. **Git**：用于保存每一次可验证的改动。

只有在重新采集论文中的呼吸信号时才需要 SerialPlot；处理已有 CSV 不需要 SerialPlot。

### 3.2 第一次运行前检查

在 MATLAB 中执行：

```matlab
ver
which iirnotch
which butter
which filtfilt
which hilbert
```

将 MATLAB 当前文件夹切换到项目根目录，也就是同时包含 `Batch_Process_Piezo.m`、`data1.m` 和 `FBG+PIO` 子文件夹的位置。脚本使用相对路径，从其他目录运行会找不到数据。

当前电脑尚未检测到 MATLAB 或 GNU Octave 命令，因此本文档完成的是代码与数据审计；第一次正式复刻时需要先安装 MATLAB 及 Signal Processing Toolbox。

---

## 4. 现有处理流程逐步拆解

```text
原始 CSV
  -> 读取第 2 列
  -> 减均值（去直流偏置）
  -> 50 Hz IIR 陷波（Q≈35）
  -> 4 阶 Butterworth 20 Hz 低通
  -> Hilbert 包络 + 200 ms 滑动 RMS
  -> 保存 6 列处理结果
  -> 首次边缘触发
  -> 起点前保留 50 ms
  -> 截取并对齐 1 s
```

### 4.1 去直流偏置

```matlab
signal = signal - mean(signal);
```

- **处理了什么**：减去整段信号平均值。
- **目的**：把基线移到 0 附近，避免传感器静态偏置影响阈值和画图。
- **验收**：处理后均值绝对值接近机器精度；波形形状和峰峰值不应改变。

### 4.2 50 Hz 陷波

```matlab
wo = 50/(fs/2);
bw = wo/35;
[b_notch,a_notch] = iirnotch(wo,bw);
signal_notch = filtfilt(b_notch,a_notch,signal);
```

- **算法**：二阶 IIR 陷波器，中心频率 50 Hz，品质因数约为 35；`filtfilt` 前后向滤波，实现零相位响应。
- **处理了什么**：电网工频及其附近的窄带干扰。
- **目的**：保留动作造成的低频变化，同时抑制当前数据中非常强的 50 Hz 成分。
- **验收**：比较滤波前后功率谱，50 Hz 峰应显著下降；动作起点不能发生可见时间偏移。

### 4.3 20 Hz 低通

```matlab
[b_lp,a_lp] = butter(4,20/(fs/2),'low');
signal_lp = filtfilt(b_lp,a_lp,signal_notch);
```

- **算法**：4 阶 Butterworth 低通，截止频率 20 Hz，零相位滤波。
- **处理了什么**：20 Hz 以上的高频噪声和快速振荡。
- **目的**：提取手势/机械运动的低频轮廓，供后续起点检测和对齐。
- **验收**：20 Hz 以上频谱能量明显降低；低频动作轮廓保留；滤波后无明显边界发散或异常振铃。

### 4.4 Hilbert 包络

```matlab
env_hilbert = abs(hilbert(signal_lp));
env_hilbert = env_hilbert ./ max(env_hilbert);
```

- **算法**：构造解析信号，取模得到瞬时幅值包络，再归一化到 0~1。
- **目的**：把正负振荡变成平滑的“活动强度”，便于观察动作持续时间和峰值位置。
- **风险**：若整段为零，除以最大值会产生 `NaN`；归一化后丢失不同文件之间的绝对幅值差异。
- **验收**：数值应在 0~1 内且没有 `NaN/Inf`；包络峰应覆盖原信号主要活动区。

### 4.5 200 ms 滑动 RMS

```matlab
win = round(0.2*fs);  % 400 点
env_rms = sqrt(movmean(signal_lp.^2,win));
env_rms = env_rms ./ max(env_rms);
```

- **算法**：400 点滑动均方根。
- **目的**：稳定估计局部能量，比单点幅值更抗噪。
- **验收**：数值应在 0~1 内且没有 `NaN/Inf`；RMS 的活动区应与 Hilbert 包络基本一致，但更平滑。

### 4.6 首次边缘触发与 1 s 对齐

`data1.m` 的核心规则为：

1. 用 0.20~0.25 s 的 100 个点估计基线均值和标准差；
2. 阈值偏移量取 `max(6×基线标准差, 0.5e-4 V)`；
3. 只在 0.25~2.00 s 内搜索；
4. 连续 10 点（5 ms）超过阈值才算触发，避免单点噪声误判；
5. 从触发点向前保留 0.05 s，再截取 1 s（2000 点）；
6. 找不到触发点或剩余长度不足时，默认从约 0.30 s 开始。

- **目的**：消除每次试验中动作开始时间的抖动，使同类样本可以逐点比较、求均值或用于分类。
- **验收**：所有输出均为 2000 行，时间为 0~0.9995 s；主要起始边缘应集中在对齐后的约 0.05 s 附近；必须记录哪些文件使用了默认起点。

---

## 5. 分阶段复刻计划与验收目标

### 阶段 0：建立数据字典和“不可变基线”

建议用时：半天到 1 天。

### 要做的事

- 向学长确认第 2.2 节列出的元数据。
- 保存原始数据文件清单、文件大小和 SHA-256；原始 CSV 只读，不在原文件上修改。
- 画出 `1-900.csv` 的原始波形和功率谱。
- 建立实验记录，写明 MATLAB 版本、工具箱版本和运行日期。

### 验收目标

- 能用一句完整的话解释一个文件名和两列数据分别代表什么。
- 知道 2000 Hz 采样率来自采集设置还是学长的假设。
- 原始文件数量为 120，全部为 10000 行，没有缺失值、重复时间点或异常列数。
- 原始数据校验值已保存，之后任何步骤都不会覆盖原始文件。

### 阶段 1：手算并复刻单文件处理

建议用时：2~3 天。

### 要做的事

- 不先运行批处理脚本，自己新建一个单文件学习脚本。
- 依次完成读取、秒时间轴、去均值、陷波、低通、Hilbert、RMS。
- 每做一步就画“处理前/后波形”和“处理前/后频谱”。
- 用 `freqz` 画陷波器、低通滤波器的幅频和相频响应。

### 验收目标

- 能解释奈奎斯特频率为何是 1000 Hz，`50/(fs/2)` 和 `20/(fs/2)` 为什么这样归一化。
- 能解释 `filter` 与 `filtfilt` 的相位差异。
- 50 Hz 峰明显被压低，20 Hz 以上能量明显下降。
- 自己的单文件结果与学长脚本对应列的最大绝对误差不超过 `1e-10`。
- 能解释 Hilbert 包络与 RMS 都表示强度，但响应速度和平滑程度不同。

### 阶段 2：复刻 120 文件批处理

建议用时：1~2 天。

### 要做的事

- 运行 `Batch_Process_Piezo.m`。
- 检查 `Results_CSV1` 与 `Results_Figure1`。
- 写一个验收脚本自动检查文件数、行数、列名、时间轴、`NaN/Inf` 和数值范围。
- 随机抽查至少 6 个文件，确保覆盖六个条件标签。

### 验收目标

- 输出 120 个处理后 CSV 和 120 张低通波形图。
- 每个处理后 CSV 为 10000 行、6 列：`Time, Raw, Notch50Hz, Lowpass20Hz, Hilbert, RMS`。
- `Time` 从 0 到 4.9995 s，步长 0.0005 s。
- 任何文件都没有 `NaN/Inf`。
- 有一张汇总表记录每个文件的原始均值、标准差、50 Hz 抑制度、低通后峰峰值和是否异常。

### 阶段 3：复刻首次边缘触发对齐

建议用时：2 天。

### 要做的事

- 先在 3~5 个典型文件上逐行调试 `data1.m`。
- 把基线区、阈值线、触发点、提前 50 ms 的起始点画在原波形上。
- 运行全部文件，保存每个文件的 `trigger_point`、`start_idx`、阈值和是否 fallback。
- 将同一条件的 20 条对齐曲线叠加，并画均值 ± 标准差带。

### 验收目标

- 输出 120 个对齐 CSV 和 120 张对齐图。
- 每个对齐 CSV 恰好 2000 行，时间为 0~0.9995 s。
- 非 fallback 样本的主要起始边缘集中在约 0.05 s，而不是散落在整个 1 s 内。
- 人工抽查的触发点与程序触发点一致；误触发和漏触发都有清单。
- 能解释为什么要用“6σ + 最小阈值 + 连续 10 点 + 限定搜索区”四层约束。

### 阶段 4：证明处理结果可重复、可比较

建议用时：2~3 天。

### 要做的事

- 对六个条件分别计算峰值、峰峰值、RMS、面积、持续时间、主频等特征。
- 画箱线图或小提琴图，观察组内重复性和组间差异。
- 做参数敏感性分析：陷波 Q 值、低通截止频率（如 10/15/20/30 Hz）、阈值倍数（4/5/6/7σ）、RMS 窗长（100/200/300 ms）。
- 比较参数变化后触发成功率、触发时间抖动和特征稳定性。

### 验收目标

- 同一参数重复运行，输出文件哈希完全一致。
- 参数不是“拍脑袋选的”，而是有频谱、误触发率和稳定性证据。
- 明确每个条件的组内变异系数，并识别异常试次。
- 在不知道条件物理含义前，只报告“条件间差异”，不做物理因果解释。

### 阶段 5：脱离原脚本独立重写

建议用时：3~5 天。

### 要做的事

- 把脚本重构为函数，例如 `load_signal`、`preprocess_signal`、`detect_onset`、`align_trial`、`extract_features`、`quality_check`。
- 把采样率、截止频率、Q 值、阈值倍数、窗口长度集中到配置结构体。
- 增加单元测试或至少增加断言。
- 用自己的代码重新生成全部结果，并与原脚本逐项对比。

### 验收目标

- 改变采样率时，所有以“秒”为单位的参数能自动换算成点数。
- 任意输入文件缺列、过短、全零或含 `NaN` 时，程序能给出明确错误或质量标记。
- 自己重写的输出与原脚本数值一致，或对差异给出有证据的解释。
- 不打开原脚本，也能从空白文件写出核心流程。

---

## 6. 论文需要掌握的结论

论文《多材料压电纤维的热拉制备方法及应用探索》的主线是：用热拉法制备柔性压电纤维，通过材料掺杂改善压电性能，再验证呼吸监测和水下探测的可行性。

### 6.1 材料与结构结论

- 纤维采用 PC 包层、CPE 导电层、P(VDF-TrFE) 压电层和铜丝电极，形成导电层—压电层—导电层三明治结构。
- 热拉后仍能保持方形结构，具备柔性和稳定性。
- 纳米 SiO₂ 掺杂会在颗粒周围形成空穴，增强压电响应。
- 论文测试的三种质量比中，SiO₂:P(VDF-TrFE)=1:15 的综合效果最好，信号强度相对未掺杂提高约 2~3 dBm；同掺杂浓度下，纤维越薄，响应越强。

### 6.2 应用结论

- 呼吸垫能够检测仰卧和侧卧呼吸，呼吸率与医用心电监护仪相比误差在 ±1 次/分钟以内，并能识别屏息阶段。
- 心跳信号可能被更强的呼吸信号覆盖，论文提出可用频域分离继续提取，但这部分更接近后续设想，不应当写成已充分验证的算法结果。
- 单根纤维可根据响应强度变化估计水下声源位置，垂直距离实验步进精度约为 2 cm。
- 双纤维/阵列可提高定位能力；论文实验中 1~17 MHz 范围内规律较好，超过 18 MHz 后位置响应不再稳定。

### 6.3 你应能回答的三个问题

1. **为什么压电纤维能测动作？** 机械形变引起压电层极化电荷变化，经导电层和电极转成电信号。
2. **为什么需要信号处理？** 压电纤维灵敏，既能测到目标形变，也会拾取工频、机械振动、接口噪声和位置差异。
3. **当前脚本解决了什么？** 它主要解决强 50 Hz 干扰、20 Hz 以上噪声、不同试次起点不一致的问题；它还没有完成特征分类、多模态融合或物理参数反演。

---

## 7. 复刻完成后如何完善

### 7.1 先完善工程可靠性

- 增加 `README.md`、数据字典、环境版本和一键运行入口。
- 原始数据、处理中间数据、最终特征、图片分目录管理。
- 输出运行日志和质量控制表，不再只在命令行打印“完成”。
- 给归一化增加零值保护；给短数据、缺列、`NaN`、异常采样间隔增加检查。
- 保存每个文件的实际触发点和 fallback 原因。
- 不把可再生的大量结果图提交到 Git；只提交代码、配置、少量示例和汇总结果。

### 7.2 再完善信号处理证据链

- 自动生成原始/陷波/低通的功率谱对比和滤波器响应图。
- 用数据决定低通截止频率，而不是固定沿用 20 Hz。
- 比较 Hilbert、RMS、Teager 能量、变化点检测等起点检测方法。
- 用中位数和 MAD 构造稳健阈值，降低基线异常点对 `mean + 6σ` 的影响。
- 用互相关或动态时间规整进一步校正波形形状差异，但要防止过度对齐抹掉真实差异。

### 7.3 建立规范的统计与识别任务

如果六个条件是类别，可按以下顺序推进：

1. 先做可解释特征：峰值、峰峰值、RMS、积分面积、上升时间、持续时间、主频、频带能量、小波能量。
2. 先做简单基线模型：逻辑回归、LDA、SVM、随机森林。
3. 数据量和受试者数量足够后，再尝试 1D-CNN、LSTM 或 Transformer。
4. 划分训练集和测试集时必须按“受试者/采集批次”分组，不能把同一次试验的相邻片段随机分到两边，否则会数据泄漏。
5. 报告混淆矩阵、宏平均 F1、置信区间和跨批次泛化，不只报告训练准确率。

### 7.4 真正实现 FBG + PIO 多模态融合

当前 CSV 尚未体现两个同步通道。补齐 FBG 后，建议依次做：

1. 统一时间戳和采样率；
2. 用同步事件或互相关估计通道时延；
3. 分别做各自适合的去噪和标定；
4. 做单模态基线，确认 FBG-only 与 PIO-only 性能；
5. 比较特征级融合、决策级融合和时序深度融合；
6. 只有融合结果在独立测试集上稳定优于两个单模态，才能得出“多模态有效”的结论。

### 7.5 与论文方向继续衔接

- 呼吸监测：延长采集时长、增加受试者和睡姿，使用参考设备同步标注，评估呼吸率 MAE、呼吸暂停灵敏度与特异度。
- 心跳分离：先用频谱确认呼吸和心跳频带，再设计带通/自适应滤波；不能仅凭屏息片段推断日常状态性能。
- 水下定位：增加纤维数量和空间方向，建立“位置—频率—响应强度”标定模型，并在独立位置测试定位误差。
- 材料实验：细化 SiO₂ 浓度、纤维厚度和重复样本数，使用统计检验和误差条验证 1:15 与厚度效应是否稳定。

---

## 8. 最终毕业式验收清单

当以下项目全部能独立完成时，可以认为已经真正学会这套数据处理：

- [ ] 能解释传感器原理、数据列、采样率和文件名标签。
- [ ] 能从原始文件独立完成去直流、陷波、低通、包络和 RMS。
- [ ] 能画频谱和滤波器响应，定量证明 50 Hz 干扰被抑制。
- [ ] 能解释零相位滤波、Q 值、截止频率、窗口长度和 6σ 阈值。
- [ ] 能独立实现边缘触发并可视化基线、阈值和触发点。
- [ ] 能批量处理 120 个文件，并自动发现缺列、异常值、失败触发和空信号。
- [ ] 能把同条件的 20 次重复汇总成均值、离散程度和异常样本报告。
- [ ] 能用新数据验证参数，而不是机械沿用 20 Hz、6σ 和 200 ms。
- [ ] 能重写模块化程序，输出与原程序一致或解释差异。
- [ ] 能清楚区分论文已经验证的结论、论文提出的设想和当前数据尚未证明的内容。
- [ ] 能建立单模态基线，并在数据齐全后完成 FBG 与 PIO 的同步和融合对比。

## 9. 推荐学习节奏

| 周次 | 重点 | 周末应交付的成果 |
|---|---|---|
| 第 1 周 | 数据字典、MATLAB 基础、单文件处理 | 单文件六步处理脚本 + 波形/频谱验收图 |
| 第 2 周 | 批处理、边缘触发、自动质检 | 120 文件处理结果 + 触发质量报告 |
| 第 3 周 | 条件统计、参数敏感性、异常分析 | 六条件汇总图 + 参数选择依据 |
| 第 4 周 | 模块化重写、单元测试、文档 | 可重复运行的新版本流程 + README |
| 后续 | 特征识别、多模态融合、论文式实验 | 独立测试集结果 + 消融实验 + 不确定性分析 |

最重要的学习顺序是：**先理解原始数据，再复刻算法；先证明每一步有效，再做分类；先建立单模态基线，再谈多模态融合。**
